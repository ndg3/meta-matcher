# Meta Matcher

Meta Matcher is a web app for finding counterpick characters in *Super Smash Bros. Ultimate*.

To run the application locally, run `docker compose up --build` in this
directory in a terminal, then point your browser to `http://localhost:8080`.
